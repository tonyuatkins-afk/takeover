/*
 * news.c - News fetcher (Phase 2: INT 63h via NetISA TSR)
 *
 * Phase 1: no implementation. News injection is handled entirely
 * by the engine using static fallback strings from .scn files.
 */

#include "news.h"
